import { extendTheme } from "@mui/material/styles";

const theme = extendTheme({
  heySonCustom: {},

  colorSchemes: {
    light: {},
    dark: {},
  },

  components: {},
});

export default theme;
