import { extendTheme } from "@mui/material/styles";

const theme = extendTheme({
  heySonCustom: {},

  colorSchemes: {
    light: {
      palette: {
        highlight: {
          main: "#ce5e46",
        },
      },
    },
    dark: {
      palette: {
        highlight: {
          main: "#ce5e46",
        },
      },
    },
  },

  components: {},
});

export default theme;
